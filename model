模块
核心模塊--第三方模塊--本地模塊
即使有一個同名的第三方模塊，也會有优先加载核心模块，核心模块只能通过模块名字引用。
非核心的通过文件路径
加载文件夹模块，会优先搜索packagejson，如果不存在就默认index，js
如果存在就搜索main里作为入口点
如果随便写一个名字，那么他会从node-modules当前目录找，逐个向上级
模块的缓存构建只执行一次。 不管你多少次调用模块，模块都只初始化一次，这就是模块缓存 27页

缓冲
用来处理二进制数据、类似图片文件之类的

事件
事件发射器

定时器
process.nextTick（callback） 进入下轮轮训